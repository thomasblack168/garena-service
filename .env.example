# Copy to /etc/garena-service/env on production (see docs/deploy.md)
GARENA_SERVICE_API_KEY=
EROSTERZ_WEBHOOK_BASE=https://erosterz.com
GARENA_WEBHOOK_SECRET=
# Optional: HTTP(S) proxy for termgame.com (Datadome blocks datacenter IPs — use Thai/residential proxy)
# TERMGAME_HTTP_PROXY=http://user:pass@proxy.example:8080
